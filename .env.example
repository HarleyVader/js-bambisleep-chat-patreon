# Patreon API Configuration
PATREON_CLIENT_ID=token
PATREON_CLIENT_SECRET=token
REDIRECT_URL=http://localhost:8888/oauth/redirect

# MongoDB Configuration for WSL Debian (optional - app will work without it)
MONGODB_URI=mongodb://127.0.0.1:27017/bambisleep_chat

# Server Configuration
SERVER_PORT=8888
SERVER_HOST=192.168.0.178
SESSION_SECRET=your_session_secret_here

# Webhook Configuration (optional)
PATREON_WEBHOOK_SECRET=token

# Tier Configuration
MY_TIER_IDS=tier_id_1,tier_id_2

# Proxy Configuration (if behind reverse proxy)
BEHIND_PROXY=false
PROXY_IP=127.0.0.1